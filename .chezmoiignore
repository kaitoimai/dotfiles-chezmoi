README.md
doc/

# ============================================================
# OS別の除外設定
# ============================================================

# macOS専用ファイル（Linuxでは除外）
{{ if not .is_darwin }}
dot_hammerspoon/
dot_Brewfile
{{ end }}

# Linux専用ファイル（macOSでは除外）
{{ if not .is_linux }}
# 今後Linux専用ファイルがあればここに追加
{{ end }}

# ============================================================
# 環境別の除外設定
# ============================================================

# サーバー環境ではGUIツールを除外
{{ if .is_server }}
dot_config/ghostty/
{{ end }}
